name: Android CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Run unit tests
        run: gradle testDebugUnitTest --stacktrace

      - name: Run Android lint
        run: gradle lintDebug --stacktrace

      - name: Build debug APK and instrumentation tests
        run: gradle assembleDebug assembleDebugAndroidTest --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: drop-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Upload unit test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drop-unit-test-results
          path: |
            app/build/reports/tests/testDebugUnitTest/**
            app/build/test-results/testDebugUnitTest/**
          if-no-files-found: warn
          retention-days: 14

      - name: Upload lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drop-lint-results
          path: |
            app/build/reports/lint-results-debug.html
            app/build/reports/lint-results-debug.xml
            app/build/reports/lint-results-debug.txt
          if-no-files-found: warn
          retention-days: 14

  screenshot-test:
    needs: build-test
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run emulator screenshot test
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: |
            gradle connectedDebugAndroidTest --stacktrace
            mkdir -p screenshots
            adb pull /data/local/tmp/drop-home.png screenshots/drop-home.png
            adb pull /data/local/tmp/drop-history.png screenshots/drop-history.png
            adb pull /data/local/tmp/drop-suggested-actions.png screenshots/drop-suggested-actions.png
            adb pull /data/local/tmp/drop-all-actions.png screenshots/drop-all-actions.png
            adb pull /data/local/tmp/drop-maps-suggestion.png screenshots/drop-maps-suggestion.png
            adb pull /data/local/tmp/drop-maps-confirmation.png screenshots/drop-maps-confirmation.png
            adb pull /data/local/tmp/drop-calendar-confirmation.png screenshots/drop-calendar-confirmation.png
            adb pull /data/local/tmp/drop-contact-confirmation.png screenshots/drop-contact-confirmation.png
            adb pull /data/local/tmp/drop-email-confirmation.png screenshots/drop-email-confirmation.png
            adb pull /data/local/tmp/drop-open-link-confirmation.png screenshots/drop-open-link-confirmation.png
            adb pull /data/local/tmp/drop-call-confirmation.png screenshots/drop-call-confirmation.png
            adb pull /data/local/tmp/drop-image-review.png screenshots/drop-image-review.png
            adb pull /data/local/tmp/drop-pdf-review.png screenshots/drop-pdf-review.png
            adb pull /data/local/tmp/drop-paste-intake-actions.png screenshots/drop-paste-intake-actions.png
            adb pull /data/local/tmp/drop-link-intake-actions.png screenshots/drop-link-intake-actions.png
            adb pull /data/local/tmp/drop-image-intake-actions.png screenshots/drop-image-intake-actions.png
            adb pull /data/local/tmp/drop-pdf-intake-actions.png screenshots/drop-pdf-intake-actions.png
            adb pull /data/local/tmp/drop-modern-link-actions.png screenshots/drop-modern-link-actions.png
            adb pull /data/local/tmp/drop-compact-price-extraction.png screenshots/drop-compact-price-extraction.png
            adb pull /data/local/tmp/drop-multiline-address-actions.png screenshots/drop-multiline-address-actions.png
            adb pull /data/local/tmp/drop-extraction-edit-restored.png screenshots/drop-extraction-edit-restored.png
            test -s screenshots/drop-home.png
            test -s screenshots/drop-history.png
            test -s screenshots/drop-suggested-actions.png
            test -s screenshots/drop-all-actions.png
            test -s screenshots/drop-maps-suggestion.png
            test -s screenshots/drop-maps-confirmation.png
            test -s screenshots/drop-calendar-confirmation.png
            test -s screenshots/drop-contact-confirmation.png
            test -s screenshots/drop-email-confirmation.png
            test -s screenshots/drop-open-link-confirmation.png
            test -s screenshots/drop-call-confirmation.png
            test -s screenshots/drop-image-review.png
            test -s screenshots/drop-pdf-review.png
            test -s screenshots/drop-paste-intake-actions.png
            test -s screenshots/drop-link-intake-actions.png
            test -s screenshots/drop-image-intake-actions.png
            test -s screenshots/drop-pdf-intake-actions.png
            test -s screenshots/drop-modern-link-actions.png
            test -s screenshots/drop-compact-price-extraction.png
            test -s screenshots/drop-multiline-address-actions.png
            test -s screenshots/drop-extraction-edit-restored.png

      - name: Upload UI screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drop-ui-screenshots
          path: screenshots/**
          if-no-files-found: error
          retention-days: 14

      - name: Upload instrumentation test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drop-instrumentation-test-results
          path: |
            app/build/reports/androidTests/connected/**
            app/build/outputs/androidTest-results/connected/**
          if-no-files-found: warn
          retention-days: 14
